# 開発ふりかえり(2026年9月)

このドキュメントは、今回の開発セッションで実装した内容と、今後同じような開発を進める際に
「最初にこう伝えてもらえるとスムーズだった」というポイントをまとめたものです。

## 1. 今回やったこと

### 一覧・UI まわり
- 商談管理・案件管理・企業一覧・担当者一覧・見積/発注・精算管理・フリーランス一覧・
  レポート・議事録の全一覧に、1ページ20件のページネーション(件数表示+ページ送り)を追加
- 設定 > ユーザー管理・権限設定(個人別)にもページネーションを追加(クライアント側)
- 「担当者一覧」(/people)が404になる不具合を修正し、サイドバーに追加
- サイドバー順序の並べ替え
- 商談・案件・フォームのURLを短縮(内部IDではなく短い連番を使うように)

### 公開問い合わせフォーム
- CRMに組み込みの公開問い合わせフォーム(`/contact/[番号]`、認証不要)を新規実装
  - 入力 → 確認画面 → 送信完了、の3ステップ構成
  - フォーム名の横に企業ロゴ表示(未設定時はCRM既定のロゴ、設定 > 会社情報で上書き可)
  - 項目ごとのプレースホルダー自動表示
  - 「個人情報の取扱いへの同意」を必須・最後の項目として自動追加(文言固定)
- フォームごとに自動返信メールを設定できる機能を追加(Resend連携、件名・本文を
  プレースホルダー付きで編集可能)

### データ品質・重複対応
- フォーム送信時、担当者の企業名を企業一覧に自動登録(同名企業があれば流用)
- 担当者のメールアドレス重複を検知し、アプリ内通知+Slack通知で管理者に知らせる仕組み
- 担当者詳細ページに、項目ごとにどちらの値を残すか選べる「統合(マージ)」機能を追加
- 既存の重複担当者(同一メールアドレス)を、一番古いものを残して整理するデータ移行
- 電話番号がハイフンなしで送信された場合の自動整形
- 依頼職種に「その他」を追加(4種類固定 → 5種類に拡張)

### 通知・連携
- Slack通知本文に `{{url}}`(対象ページへのリンク)・`{{mentions}}`(担当者への
  メンション)を差し込めるように機能拡張
- 設定 > ユーザー管理でSlack IDを登録できるように

### デプロイ
- 長期間mainブランチに反映されていなかった複数のfeatureブランチの内容をマージし、
  Vercel経由で本番デプロイを実施

## 2. 発生した不具合とその原因

正直に振り返ると、以下の不具合が発生しました。今後の教訓の参考として記録します。

- **選択式項目(依頼職種・個人情報同意)の値が管理画面の編集で壊れる不具合**:
  「選択肢」を自由編集できる汎用UIを、固定値を前提とするビルトイン項目にもそのまま
  使ってしまったことが原因。修正には、データ復旧のマイグレーションが2回必要になった
- **担当者検索のPostgRESTフィルタインジェクション**: 検索語をそのままクエリ文字列に
  埋め込んでいたため、セキュリティレビューで指摘を受けて修正
- **通知が届かない不具合**: 原因の多くは「マイグレーション未適用」「通知設定が
  デフォルト無効のまま」など、コードではなく設定・適用漏れによるものだった
- **本番URLで古いページが表示される**: pushし忘れ・デプロイ未実行により、
  マージ済みの最新コードではなく古いデプロイが表示されていた

## 3. 今後の教訓:最初にこう伝えてもらえるとスムーズだったこと

### (1) 固定の選択肢を持つ項目は「編集不可」だと最初に明言する
「依頼職種」のような、値がDB制約やコード側のロジックと結びついている項目は、
「選択肢は変更できない/変更したい場合は開発側に相談してほしい」と最初に伝えてもらえると、
汎用編集UIをそのまま適用してしまう事故を防げます。

### (2) 「固定の4種類」のような区分は、将来増える前提かどうかを最初に決める
依頼職種のように後から「その他」を追加する可能性がある区分は、最初から
「将来項目が増える可能性がある」と伝えてもらえれば、DBの制約(check制約)で
固定するのではなく、変更しやすい設計にできました。

### (3) 重複データの扱い方針(削除/統合/通知のみ)を最初に決めておく
「重複したらどうしたいか」(自動で消す、通知だけする、統合できるようにする、
関連データ(商談など)は残すか消すか)を先に決めてもらえると、実装の手戻りが減ります。
今回は「通知→手動統合」に落ち着きましたが、最初の指示は「削除」でした。

### (4) 外部サービス連携(メール送信など)は、使うサービスを先に決めておく
自動返信メールのように外部サービス(今回はResend)が必要な機能は、契約・APIキー発行に
時間がかかることがあるため、機能の相談と同時に「どのサービスを使うか」を決めておくと、
開発と並行してアカウント準備を進められます。

### (5) 「全部の一覧に」のような一括対応は、対象一覧を列挙してもらえると漏れにくい
ページネーションのように「全ての一覧に適用してほしい」という指示の場合、対象の一覧名を
列挙してもらえると(またはこちらから列挙して確認を取ることを徹底すれば)、
1つ見落とす(今回はフォーム管理一覧)ようなことを防げます。

### (6) デプロイのタイミング・頻度を早めに決めておく
今回は開発の最後にまとめて本番デプロイしたため、mainブランチが長期間古いままになり、
デプロイ作業自体で確認事項(ブランチ戦略、Supabaseの認証設定、環境変数)が多く発生
しました。「機能ごとに都度デプロイする」「週次でデプロイする」など、早い段階で
デプロイの運用ルールを決めておくとスムーズです。

### (7) 通知機能は「誰に」「どこに」「何を含めて」を最初にセットで伝える
通知(Slack・アプリ内)を作る際、「誰に届けるか」「Slack本文にURLやメンションを
入れたいか」を最初にまとめて伝えてもらえると、後から機能を継ぎ足す形にならずに済みます。

## 4. デプロイ・運用の際のチェックリスト(次回以降の参考)

- [ ] 未適用のマイグレーションがないか(`supabase/migrations/`の最新ファイルまで実行済みか)
- [ ] Vercelの環境変数(本番)に新しいAPIキー等が登録されているか
- [ ] Supabase の Authentication > URL Configuration が本番URLに対応しているか
- [ ] `git push origin main` 後、Vercelの Deployments で最新コミットが「Ready」になっているか
- [ ] 設定 > 通知設定 で、新しい通知イベントが必要に応じて有効化されているか
