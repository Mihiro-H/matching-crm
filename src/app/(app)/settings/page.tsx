import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="設定"
      description="通知設定・権限設定(個人別/部署一括)を行う画面です(管理者限定)。"
    />
  );
}
